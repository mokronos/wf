@AGENTS.md

- Use codex (codex exec) with gpt-5.5 when you want to implement something. Check remaining usage beforehand with `codexbar`.
- Review codex results yourself afterwards, and let it make improvements if needed.
